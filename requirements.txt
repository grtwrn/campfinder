# campfinder runs on the Python standard library alone for everything except
# the optional headless-browser fallback in browser.py.
websockets>=12.0   # only needed for browser.py (Chromium over DevTools Protocol)
openpyxl>=3.1        # only needed for plan.py (the trip planner)
